# AIBrainIM 开发进度

## 2026-06-10 P0 任务

### P0-1: 修复键盘无法收起 ✅ 完成
- **Commit**: c0fe344
- **文件**: src/screens/ChatScreen.tsx
- **改动**:
  1. 添加 `Keyboard` 和 `TouchableWithoutFeedback` 到 react-native imports
  2. `ScrollView` 的 `keyboardShouldPersistTaps` 从 `"handled"` 改为 `"always"`
  3. `KeyboardAvoidingView` 被 `TouchableWithoutFeedback onPress={Keyboard.dismiss}` 包裹

### P0-2: 恢复登录界面（Keychain token 校验）✅ 完成
- **Commit**: c0fe344
- **文件**: src/App.tsx, src/screens/LoginScreen.tsx
- **改动**:
  1. App.tsx: `useState(true)` → `useState(false)` + 启动时检查 Keychain token
  2. 新增 loading 过渡状态（"正在验证身份..."）
  3. `handleLogout` 清除 Keychain token
  4. LoginScreen: 登录成功后保存 token 到 Keychain

### P0-3: 远程 gateway 连通性验证 ✅ 完成（重大架构发现）
- **Commit**: c0fe344, 2adc89a
- **文件**: src/services/RemoteConnectionTester.ts
- **关键发现**:
  1. ✅ Gateway URL `https://node.tail67ac15.ts.net` HTTP 可达
  2. ✅ `sessions_list` 工具正常工作（正确 tool name: `sessions_list`，不是 `sessions`）
  3. ❌ `POST /api/sessions/{key}/messages` 返回 404（路径不存在）
  4. ❌ `sessions_send` 工具被 gateway 硬拒绝（安全策略）
  5. ❌ `message` 工具不存在于 HTTP API
  6. ❌ Gateway 仅监听 loopback (127.0.0.1:18789)
  7. ❌ Tailscale serve 不支持 WebSocket 升级
  8. ❌ Hermes 不是独立 session（是 zhuli 内部同步的）
- **架构结论**: 
  - AIBrainIM 的 REST 消息发送路径根本不存在
  - 唯一可行远程消息通道：Feishu 消息 API
  - Hermes 是 zhuli 的内部共享层，不是独立 AI

### P0-4: Hermes 接入 ⚠️ 需架构调整
- **状态**: 发现 Hermes 不是独立 session，需要重新设计
- **待办**: 
  1. Hermes 模式改为在 zhuli session 中使用不同 system prompt/context 标记
  2. AI 模式切换 chip UI 仍可在 ChatScreen 添加（只是运行时 context 不同）
  3. 消息通过 Feishu 消息 API 发送（而非 gateway REST）

## 阻塞项汇总

| # | 阻塞项 | 影响 | 解决方案 |
|---|--------|------|---------|
| 1 | Gateway 仅监听 loopback | iOS 无法远程连接 | 修改 `bind: "all"` 或 SSH 反向隧道 |
| 2 | sessions_send HTTP 禁止 | 无法发消息 | 改用 Feishu 消息 API |
| 3 | Hermes 无独立 session | "切换 Hermes"功能需重新设计 | 用 zhuli + 不同 context 标记代替 |

## TypeScript 编译状态
✅ `npx tsc --noEmit` 通过，无错误
