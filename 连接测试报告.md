# AIBrainIM 远程连接测试报告

> 测试时间：2026-06-10 08:24 CST
> Gateway：https://node.tail67ac15.ts.net
> Session Key：agent:zhuli:feishu:direct:ou_9782bd16e99998d38b13d05ff5cb648c

## 总体状态：PARTIAL

## 关键发现

### ✅ Gateway HTTP 可达性
- Tailscale serve URL `https://node.tail67ac15.ts.net` 可以从 Mac mini 访问
- `/tools/invoke` 接口正常工作
- `sessions_list` 工具调用成功，返回 100 个 session

### 🔴 致命问题：sessions_send 被 HTTP 拒绝

**OpenClaw Gateway 将 `sessions_send` 放入 HTTP `/tools/invoke` 的硬拒绝列表**（出于安全原因）：

> 原文：*"sessions_send — cross-session message injection — is in the hard deny list by default"*

**这意味着通过 HTTP POST 无法直接发送消息到 session。**

- 尝试 `sessions_send` → `{"ok":false,"error":{"type":"not_found","message":"Tool not available: sessions_send"}}`
- 尝试 `POST /api/sessions/{key}/messages` → HTTP 404（该路径不存在）

### 🔴 致命问题：Gateway 仅监听 Loopback

```
bind: loopback (127.0.0.1)
port: 18789
```

Gateway 只监听本机回环地址，Tailscale serve 仅代理 HTTP 静态内容和 `/tools/invoke`，**不支持 WebSocket**。

### 🔴 Hermes 未找到

在 sessions_list 返回的 100 个 session 中，**没有 Hermes 相关的 session**。

## 架构问题总结

| 问题 | 严重程度 | 说明 |
|------|---------|------|
| `sessions_send` HTTP 禁止 | 🔴 致命 | 无法通过 HTTP 发送消息 |
| WebSocket 未暴露 | 🔴 致命 | gateway 只监听 loopback |
| Hermes session 不存在 | 🔴 致命 | 无法向 Hermes 发消息 |
| Tailscale serve 不支持 WS | 🔴 致命 | iOS App 无法远程连接 |

## 建议与后续步骤

### 🔧 立即可执行（Mac mini 侧）

1. **设置 SSH 反向隧道**（推荐方案）：
   ```bash
   # 在 Mac mini 上运行（或配置 LaunchAgent）
   ssh -fN -R 18789:127.0.0.1:18789 user@<iOS设备IP>
   ```
   或使用 Tailscale 的 `tailscale serve --bg` 配合 `tailscale funnel`

2. **修改 Gateway 绑定到所有接口**（安全性降低）：
   在 `~/.openclaw/openclaw.json` 中修改：
   ```json
   {
     "gateway": {
       "bind": "all",
       "port": 18789
     }
   }
   ```
   然后重启 `openclaw gateway restart`
   ⚠️ 警告：这会让 gateway 在公网可访问，需要配合防火墙和 HTTPS

3. **确认 Hermes 部署情况**：
   - Hermes 是否作为独立进程运行？
   - Hermes 的 session key 是什么？
   - 在 Mac mini 上运行 `openclaw sessions list | grep hermes` 查找

4. **为 iOS App 配置专用连接方式**：
   - 通过 Feishu 消息通道（现有 `directMode=false` 回退）
   - 或通过 WebSocket 连接到 gateway

### 📋 Hermes Session 查询
当前 sessions_list 中的 session key：
- `agent:zhuli:feishu:direct:ou_9782bd16e99998d38b13d05ff5cb648c` ← 当前配置
- 无 Hermes 相关 session

---
*此报告由 AIBrainIM RemoteConnectionTester 自动生成*

## 补充发现：Hermes 根本不是独立 session

搜索 sessions_list 返回的 100 个 session，**没有任何 Hermes 相关的 key**。

根据 cron label 和 cron 名称判断：
- `agent:zhuli:cron:45e5e8cb-e6fd-4c44-afbf-9b67763af689` — **"Cron: 【共享层】OpenClaw ↔ Hermes 同步（5min）"**

这说明 **Hermes 和 zhuli 是通过内部共享层同步的**，不是独立 session。**"切换到 Hermes"功能实际上没有独立的 session key**，用户实际上是在和同一个 zhuli agent 说话，只是 context 不同。

### 结论：AIBrainIM 的 messaging 架构需要重新设计

| 当前做法 | 问题 |
|---------|------|
| `POST /api/sessions/{key}/messages` | 路径不存在，HTTP 404 |
| `gatewayInvoke('sessions_send', ...)` | 被 gateway 硬拒绝（安全策略） |
| `gatewayInvoke('message', ...)` | 工具不存在 |

**唯一可行的远程消息通道：Feishu 消息 API（`feishu_im_user_message`）**

建议：AIBrainIM 在 `directMode=true` 时，实际应该通过 Feishu 消息 API 发送消息到 bot，而不是试图直接调 gateway REST API。
